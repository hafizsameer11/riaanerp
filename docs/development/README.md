# Development helpers

## Windows: Composer with XAMPP PHP

If `composer` fails because `php` is not on your PATH, use the batch file from the **project root**:

```bat
docs\development\composer-xampp.bat install
```

See **`../docker/setup.md`** for full context (Composer section).
